/*
*OPERATOS are caracters with diferents uses in JS

 ? Arithmetic operators
    ! + adition
    ! - subtraction
    ! * multiplication
    ! / division
    ! % modulus
    ! ++ increment 
    ! -- decrement

 ? Assignment operators
    !  = example; x = y    same as; x = y
    ! += example; x += y   same as; x = x + y
    ! -= example; x -= y   same as; x = x - y
    ! *= example; x *= y   same as; x = x * y
    ! /= example; x /= y   same as; x = x / y
    ! %= example; x %= y   same as; x = x % y

 ? Comparison operators
    ! == equal to
    ! === equal value and equal type
    ! != not equal
    ! !=== not equal value or not equal type
    ! > greater than 
    ! < less than
    ! >= greater than or equal to
    ! <= less than or equal to
    ! ? ternary operator

 ? Logical operators
    ! && logical AND 
    ! || logical OR
    !  ! logical NOT 
 */

 // TODO: w3schools.com/js/
 // TODO: developer.mozilla.org/en-US/docs/Web/JavaScript